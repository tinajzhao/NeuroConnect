# Installation Guide
1. Clone the repository and create a Python environment.
2. Install dependencies:
```bash
# For neuroconnect features
pip install -r requirements_shiny_neuroconnect.txt
```
3. Run the app:
```bash
shiny run --reload app_shiny_neuroconnect.py
```
